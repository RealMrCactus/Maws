[package]
name = "maws"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rand = "0.9.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
 
